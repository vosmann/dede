source dede.conf

ssh -i $SSH_KEY_PATH $USER@$HOST "mongodump --out $DUMP_SERVER_LOCATION/$DUMP_DIR_NAME"
ssh -i $SSH_KEY_PATH $USER@$HOST "tar -cvzf $DUMP_SERVER_LOCATION/$DUMP_FILE_NAME $DUMP_SERVER_LOCATION/$DUMP_DIR_NAME"

scp -i $SSH_KEY_PATH $USER@$HOST:$DUMP_SERVER_LOCATION/$DUMP_FILE_NAME $DUMP_LOCAL_LOCATION/$DUMP_FILE_NAME$DUMP_FILE_NAME

