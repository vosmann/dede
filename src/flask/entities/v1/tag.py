class Tag:
    """A tag is used to describe the category a project or news entry belongs
    to."""
    def __init__(self, _id, display_text):
        self._id = _id
        self.display_text = display_text
 
